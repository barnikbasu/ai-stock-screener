[project]
name = "ai-stock-screener"
version = "0.1.0"
description = "A lightweight LangGraph stock screening workflow using Yahoo Finance"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langgraph>=0.6.1",
    "yfinance>=0.2.65",
]

[tool.uv]
package = false
